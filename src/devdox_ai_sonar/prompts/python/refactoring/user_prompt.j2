Fix this {{ language }} SonarQube cognitive complexity (S3776) issue in `{{ file_path }}` by editing the file directly using your tools.
Do NOT output JSON. Do NOT paste code in your reply.

To apply the change, use `fix_at_line`:
- Call `file_editor view` first with a `view_range` covering the function below — you need its current content.
- Apply ONE `fix_at_line` call to replace the original function with the simplified version (the one that calls helpers). Use the function's exact line range as `start_line`/`end_line`, and `old_block` set to the current content of those lines (exactly as `view` showed them).
- Then apply ONE additional `fix_at_line` call PER helper to insert it after the original function. For each insertion, set `start_line` and `end_line` to the line immediately after the original function (anchor on its current content via `old_block`, and prepend the helper's source to `new_block`).
- `fix_at_line` anchors by line number, so duplicated lines elsewhere in the file will not cause failures.
- Do NOT use `file_editor str_replace` — it is unsafe when the file has duplicated lines.

## Issue
{% for issue in issues %}
- **Rule**: {{ issue.rule }}{% if issue.severity %} ({{ issue.severity }}){% endif %}
- **Message**: {{ issue.message }}
{% set root = rule_info.get(issue.rule, {}).get('root_cause', '') %}
{% if root %}- **Root cause**: {{ root }}{% endif %}
{% endfor %}


## 🚨 COMPLEXITY TARGET

Current: {{ issues[0].message.split('from ')[1].split(' to')[0] if 'from' in issues[0].message else '?' }} → Target: <15

MANDATORY:
1. Extract one or more helpers and place them as siblings (immediately after the original function)
2. The original function (with its name and signature unchanged) MUST call ALL helpers
3. The refactored result MUST reach the target

## CODE
{% for func in context.context_dict['new_context']%}### `{{ func['function_name'] }}` (L{{ func['start_line'] }}-{{ func['end_line'] }})
`````{{ language }}
{% for line in func['context'].split('\n') %}{{ '%04d' | format(func['start_line'] + loop.index0) }}| {{ line }}
{% endfor %}```
{% endfor %}


{% if context.context_dict['import_section'] and context.context_dict['import_section']['has_imports'] %}## IMPORTS
````{{ language }}
{{ context.context_dict['import_section']['content'] }}```
{% endif %}

{% if error_message %}## ⚠️ Previous attempt failed — also fix this
{{ error_message }}
{% endif %}

## RULES
- Keep exact function name and signature
- Extract helpers, use early returns
- NO new attributes/methods/classes/exceptions (other than the extracted helpers)
- NO imports inside functions

{{ method_instruction }}

Read `{{ file_path }}` with `file_editor view` first, then apply the fixes with `fix_at_line` (one call to replace the original function, one call per helper to insert it as a sibling), then run `python -m py_compile {{ file_path }} && echo "SYNTAX_OK"` to confirm syntax is valid.
